import { Color } from '../../interface';
export declare class OAIProgressIndicator {
    /** (optional) The size of the progress indicator (xs (default) / sm / lg / xl) */
    size: 'xs' | 'sm' | 'lg' | 'xl';
    color?: Color;
    render(): any;
}
